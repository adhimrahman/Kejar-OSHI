<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kejaroshi">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/spotitip_logo__4_"
        android:label="@string/app_name"
        android:roundIcon="@drawable/spotitip_logo__4_"
        android:supportsRtl="true"
        android:theme="@style/Theme.KejarOSHI"
        tools:targetApi="31">

    <!-- SplashScreen sebagai layar awal -->
    <activity
        android:name="com.example.kejaroshi.MainActivity"
        android:exported="true"
        android:theme="@style/Theme.KejarOSHI">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

</application>

    </manifest>
